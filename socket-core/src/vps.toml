# Registro de VPS disponibles para el Agente SSH.
# Añadir un VPS nuevo = añadir un bloque [[vps]] aquí y reiniciar el contenedor.
# NO hace falta recompilar Rust.

[[vps]]
id = "vps1"
host = "161.97.146.80"              # o IP pública, o IP del otro VPS
user = "deploy"
ssh_key_path = "/app/keys/vps1.pem"
port = 22

[vps.commands]
ram = "free -h"
disk = "df -h"
uptime = "uptime"
top_procesos = "ps aux --sort=-%mem | head -10"

# ho
# --- Ejemplo de un segundo VPS ---
# [[vps]]
# id = "vps2"
# host = "203.0.113.20"
# user = "deploy"
# ssh_key_path = "/app/keys/vps2.pem"
# port = 22
#
#   [vps.commands]
#   ram = "free -h"
#   disk = "df -h"
